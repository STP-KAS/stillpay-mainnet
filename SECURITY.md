# Security

MAINNET SHAPE. NOT USD. THIS REPO DOES NOT SUBMIT TRANSACTIONS.

- Never paste a seed into this site or this repo.
- `.local/` is gitignored.
- Preview binds `127.0.0.1` and allowlists paths. It must not serve `.git`.
- This repo refuses `kaspatest:` addresses.
- Broadcast functions require `STILLPAY_I_UNDERSTAND_MAINNET=1` and `STILLPAY_BROADCAST=1` and still do not sign.
- Report via GitHub Security advisory. Do not DM keys.
